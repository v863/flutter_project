import 'package:flutter/material.dart';

class MyFarmerPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Farmer Page"),
      ),
      body: Center(
        child: Text("Hello World"),
      ),
    );
  }

}